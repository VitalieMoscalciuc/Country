CREATE TABLE IF NOT EXISTS country (
                                    id serial PRIMARY KEY,
                                    name VARCHAR(255),
                                    country_code VARCHAR(5)
);