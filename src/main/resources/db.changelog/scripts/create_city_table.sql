CREATE TABLE IF NOT EXISTS city (
                                    id serial PRIMARY KEY,
                                    name VARCHAR(255),
                                    people_count INT
);